# Configuration file for libgpiod-2.1 package
# This file is sourced by build-lib.sh script

# Package-specific build configuration
PACKAGE_NAME="libgpiod-2.1"
PACKAGE_VERSION="2.1"

# Build options (override default configure options if needed)
CONFIGURE_OPTS="--enable-static=no --enable-tests=no"

# Additional environment variables (optional)
# export CFLAGS="-O2 -g"
# export LDFLAGS="-L/path/to/libs"

pre_extract() {
    echo "Setting up environment for libgpiod autogen.sh"
    export NOCONFIGURE=1
}

# Pre-build commands (optional)
# pre_build() {
#     echo "Running pre-build steps for $PACKAGE_NAME"
#     # Add any pre-build commands here
# }

# Post-build commands (optional)
# post_build() {
#     echo "Running post-build steps for $PACKAGE_NAME"
#     # Add any post-build commands here
# }